kelp_models:
  - name: mat_overwrite_orders
    description: Fixture model for overwrite materialization tests
    columns:
      - name: id
        data_type: bigint
        nullable: false
      - name: name
        data_type: string
        nullable: true
    materialization:
      write_mode: overwrite
