kelp_models:
  - name: mat_append_incremental_orders
    description: Fixture model for ctx-based incremental append materialization tests
    columns:
      - name: id
        data_type: bigint
        nullable: false
      - name: event_ts
        data_type: bigint
        nullable: false
      - name: name
        data_type: string
        nullable: true
    materialization:
      write_mode: append
