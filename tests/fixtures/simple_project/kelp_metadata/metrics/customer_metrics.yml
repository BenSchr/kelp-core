kelp_metric_views:
  - name: customer_metrics
    definition:
      version: 1.1
      comment: Customer metrics view
      source: ${ catalog_name }.${ schema_name }.orders
      fields:
        - name: customer_id
          expr: customer_id
          comment: Customer identifier
      measures:
        - name: total_orders
          expr: count(*)
          comment: Total number of orders
        - name: total_amount
          expr: sum(amount)
          comment: Total order amount
